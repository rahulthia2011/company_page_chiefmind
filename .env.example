# URL of the Cloudflare Worker intake endpoint. Must be HTTPS in production.
VITE_INTAKE_ENDPOINT=https://api.chiefmind.io/intake

# Cloudflare Turnstile site key (public). Get from dash.cloudflare.com > Turnstile.
# Leave unset in dev to skip the widget entirely.
VITE_TURNSTILE_SITE_KEY=
